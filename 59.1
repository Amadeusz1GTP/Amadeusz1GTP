#include <iostream>
#include <math.h>
#include <fstream>
 
using namespace std;
 
 int liczba ;
 int x= 0;
 int y= 2;
 
    while(liczba>0)
    {
        if(liczba%i==0)liczba=liczba/2;
        else
        return 0;
        
        for(int j=1;x>=j;j++)
        {
            if((i>2)&&(i%j==0))i++;
        }
        if(x==2)i++;
    }
    if(y==3) return 1;
    else return 0;
}
int main()
{
    fstream plik;
    plik.open("liczby.txt");
 
    int liczba;
    int x= 0;
 
    while(!plik.eof())
    {
        plik>>liczba;
    }
    cout<<"Wynik;"<<endl;
 
    return 0;
}

